#AVML
./avml memory.lime

#afterwards provide the file memory.lime for analysis (it should have the size of the installed memory of the machine)

#LiME (alternative tool)
sudo insmod lime-4.13.0-36-generic.ko path=memory.raw format=raw

#afterwards provide the file memory.raw for analysis (it should have the size of the installed memory of the machine)
